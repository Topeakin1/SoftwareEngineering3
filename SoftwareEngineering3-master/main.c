#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include "Assignment3Header.h"

int main(void) {
	srand(time(NULL));
	Player players[PLAYER_MAX]; 
    Slot slots[SLOT_MAX];
    
    int slotsCount;
	int playersCount;    
    
}
